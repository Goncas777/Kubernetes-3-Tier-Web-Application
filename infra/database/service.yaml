apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None  # Headless service (necessário para StatefulSet)
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: postgres
